# WARNING: This is a placeholder. Generate the actual file from the NixOS installer USB.
# Boot the installer, partition your disk, then run:
#   nixos-generate-config --no-filesystems
# Then copy /etc/nixos/hardware-configuration.nix here.

{
  imports = [ ];
}
